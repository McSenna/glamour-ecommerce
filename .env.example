# Optional — omit to use Vite dev proxy (`/api` → http://127.0.0.1:5002)
# VITE_API_URL=http://127.0.0.1:5002/api

# When Express listens on another host/port, point the dev proxy there (must match server/.env PORT)
# VITE_DEV_PROXY_TARGET=http://127.0.0.1:5002
